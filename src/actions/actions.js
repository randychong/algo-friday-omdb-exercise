export const changeUsername = (dispatch) => {
    return dispatch({ type: "CHANGE_USERNAME"})
}

export const addUser = (dispatch) => {
    return dispatch({ type: "ADD_USER"})
}

export const loadTrue = (dispatch) => {
    return dispatch({ type: "LOAD_TRUE"})
}

export const loadState = (dispatch) => {
    return dispatch({ type: "LOAD_STATE"})
}